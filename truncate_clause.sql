-- delete all data from the table persons

truncate table persons      -- after truncation, data is deleted but table still eixts with the same structure

select * from persons

-- truncate clause doesn't support 'where' clause which only deletes specific row data